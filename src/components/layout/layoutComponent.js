import React from "react"
import { Header } from "components/UI"

const LayoutComponent = ({ children, theme, data }) => {
  return (
    <React.Fragment>
      
    </React.Fragment>
  )
}

export default LayoutComponent
